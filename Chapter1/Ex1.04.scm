;; to explain the procedure a plus absolute b
;; whereby operators are compound expressions
;; depending on the predicate (> b 0)
;; if b > 0, the operator is "+"
;; otherwise the operator is "-"
;; finally the opertor is applied on the 2 operands a and b
